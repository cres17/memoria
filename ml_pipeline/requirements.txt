# Neural Color Transfer — ml_pipeline 의존성
# Python 3.9+ 권장

# 딥러닝
torch>=2.1.0
torchvision>=0.16.0
timm>=0.9.12
# Pinned local runtime for MediaPipe Selfie Multiclass semantic-mask cache.
ai-edge-litert==2.1.6

# 수치 계산
numpy>=1.24.0
scipy>=1.11.0

# 이미지 처리
Pillow>=10.0.0
imageio>=2.31.0

# 학습 유틸
tqdm>=4.65.0

# ONNX 변환
onnx>=1.14.0
onnxruntime>=1.16.0

# TFLite 변환은 Python 3.11 전용 requirements-tflite.txt를 사용한다.

# CoreML 변환 (선택, Mac 전용)
# coremltools>=7.0

# 이미지 다운로드
requests>=2.31.0
