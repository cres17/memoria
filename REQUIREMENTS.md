# Memoria 앱 개선 요구사항

> 범위: **당장 고칠 제품 버그/UI 요구**. 장기 엔진 아키텍처·기능 명세·품질 게이트는 `SNAPSEED_GRADE_EDITOR_SPEC.md` 참조. 두 문서를 섞지 말 것.

## 1. 필터 탭 정리

### 1-1. 기본 필터 제거
- 후지필름(Fujifilm)과 라이카(Leica) 필터만 남기고 나머지 기본 필터 전부 삭제

### 1-2. 필터 미리보기 방식 변경
- 현재: 편집 중인 사진에 필터를 미리 적용해서 보여줌
- 변경: 각 필터 고유의 색감을 나타내는 고정 샘플 이미지로 표시

### 1-3. 필터 이름 → 브랜드 로고로 교체
- 필터명이 너무 길어 가독성이 낮음
- 각 필터 미리보기 썸네일 **왼쪽 상단**에 브랜드 로고(Fujifilm / Leica) 삽입
- 텍스트 이름은 간략하게 줄이거나 로고로 대체

---

## 2. 인물(Portrait) 탭

- 세그멘테이션 선택 버튼들을 **가운데 정렬** 처리

---

## 3. 합성(Blend/Overlay) 탭

- 이미지 선택 버튼 크기를 텍스트(단어) 길이에 맞게 **축소** (현재 불필요하게 큰 상태)

---

## 4. 이미지 크기 대응 (레이아웃 버그)

- 기본 설정된 사진 비율/사이즈가 아닌 경우 이미지가 **밀리거나 상단이 잘리는** 버그 수정
- 다양한 해상도 및 가로세로 비율에서도 레이아웃이 정상 유지되어야 함

---

## 5. 도구(Tools) 탭 — 실시간 미리보기 품질

- **크롭(Crop)**, **회전(Rotate)**, **원근(Perspective)** 조작 시 캔버스 반영이 느리거나 끊겨 보이지 않도록 개선
- 슬라이더/핸들 조작 즉시 캔버스에 결과가 반영되는 수준의 실시간성 보장

---

## 6. 커브(Curve) 에디터 — 채널 버튼 가시성

- L(Luminance)과 RGB 채널 전환 버튼이 **선택 상태일 때 색이 잘 구분되지 않음**
- 선택/비선택 상태의 색상 대비를 높여 명확히 구분되도록 스타일 수정

---

## 7. 필터 만들기(Create Filter) — 색 반전 버그

- 커스텀 필터 생성 시 색이 반전되는 경우가 발생
- LUT 생성 파이프라인 또는 적용 로직의 채널 순서/범위 오류 확인 및 수정
